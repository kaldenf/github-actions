name: first

on:
  pull_request:
    branches: ['main']

jobs:
  check_for_linked_tickeet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Check for linked ticket
        uses: ./.github/actions/check-description
        with:
          who-to-greet: 'Mona the Octocat'


