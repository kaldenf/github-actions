name: Linked JIRA Ticket

on:
  pull_request:
    branches: ['main']

jobs:
  read_pr_description:
    runs-on: ubuntu-latest
    steps:
      - name: Check for linked ticket
        uses: ./read-descriptions
        with:
          who-to-greet: 'Kalden'


