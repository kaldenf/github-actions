name: Linked JIRA Ticket

on:
  pull_request:
    branches: ['main']

jobs:
  read_pr_description:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check for linked ticket
        uses: ../action
        with:
          who-to-greet: 'Kalden'


